name: Node.js CI

on:
  push:
    branches: Latest
  

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: npm install
    - run: npm run build 
    - run: npm start & npx wait-on http://localhost:3000
    - run: npm stop
